# omp-parity 任务进度总结(用户口径)

- **日期**:2026-08-21 初版 / **2026-08-22 更新**(批次 7 扩展宿主面 + 真机 UI 扫描 + 三缺陷修复 + /undo 预填与压缩 loader 复验闭环 + agent-definitions 数据面重构)
- **基准**:批次 1–5 验收(08-20);批次 7 E01/E02/E08 落地并 live 验收(08-21 深夜);真机 CDP 扫描 + 三缺陷修复 + 第二档清欠两项(08-22);agent-definitions 换轨 omp 发现链 + persona 修复(08-22 晚,证据 99 §6.17);观察门计时中(起点 2026-08-20,最早 2026-09-03 验)
- **配套**:`00-MASTER.md` §8 批次计划(含批次 7 行、09 章);`99-ACCEPTANCE-EVIDENCE.md` §5–6 逐项总账与验收证据(最新 §6.17)
- **读法**:按「能不能用 + 测没测」分四档,不是按 spec 编号
---

## 总账(还有多少没完成)

| 状态 | 数量 | 明细所在 |
|---|---|---|
| ✅ 完成且验证过 | **20** | 第一档(逐项列出) |
| 🟡 做完但没验证 | **10** | 第二档(逐项列出) |
| 🟠 一半,卡 omp 引擎 | **8** | 第三档(逐项列出) |
| ❌ 完全没做 | **31** | 第四档:10 旧面删除 + 18 P2 新功能 + 3 批次 7 尾款 |
| ⏳ 进行中 | **1** | 观察门(2026-09-03 起验) |
| **合计** | **70** | **未完成 = 70 − 20 = 50**(10 待验 + 8 卡引擎 + 31 没做 + 1 门) |

未完成 50 项的构成:10 项等真实状态/三端实跑(日常使用顺手验)、8 项等 omp 引擎补端点、10 项等观察门后删除、18 项 P2 未排期、3 项批次 7 尾款可随时做、1 项观察门计时中。
## 第一档:界面上能完整体验,已亲自测试 ✅ tested

**聊天输入框 / 会话里:**

1. **审批弹窗** —— 非 yolo 会话跑工具会弹窗,批准 / 拒绝 / 「始终允许」(先写设置成功才批准)全部真实走通(浏览器驱动 + 修复 8 个缺陷后复验)
2. **ask 提问弹窗** —— 同上,真实 SDK 弹窗驱动过
3. **思考等级菜单** —— 选 Max → 落 transcript → 重载后输入框仍显示 "Max"(完整链路验证)
4. **模型选择器** —— 角色轨 + 全模型列表双面;enabledModels 白名单过滤 + 乱配警示行
5. **「设为角色」** —— UI 里把 tiny 角色指到某模型,`config.yml` 真实变更
6. **发消息不带模型污染** —— 角色跟随,抓包验证发送体无 model
7. **扩展 widget 投影**(批次 7)—— ✅ 08-22 真机视觉确认:输入框上方五行 zhipu 配额条逐字可见、实时自刷新;`/zhipu-usage off` 清除链路服务端验证
8. **`/zhipu-usage` 扩展命令识别**(08-22 修复)—— 用户扩展的 `pi.registerCommand` 命令进注册表(`source:"extension"`)+ 自动补全实测出现(此前 headless 发现机制天然不含扩展命令)
9. **persona 选择器菜单** —— ✅ 08-22 真机过:modes.v1 下 persona 区在模式菜单内,开合正常
10. **斜杠自动补全合并** —— ✅ 08-22 真机过:`/` 菜单含 `/tree`、`/troubleshoot`(引擎层)与 `/zhipu-usage`(扩展层)
11. **`/tree` 会话树对话框** —— ✅ 08-22 复核闭环:有回合会话 500ms 打开、内容稳定(此前"卡 Loading"为 Enter×菜单交互竞态误报)
12. **notice toast** —— ✅ 08-22 真机偶得:MCP 挂载通知以 toast 呈现(omp.notice.raised → toast 链路实跑)

**设置页:**

13. **引擎设置页** —— 各项修改真实写入 `config.yml` 落盘验证
14. **Defaults 角色编辑器** —— 10 个角色赋值/清除,服务端真值变化验证(含修复 Clear 按钮未绑 onClick 的缺陷)

**命令 / 数据面(curl 实测,界面消费):**

15. **自定义 agent 增删改** —— 数据面已换轨 omp 发现链(08-22 重构,证据 99 §6.17):列表 = `discoverAgents` 真值(用户层 + bundled + 项目层),写入 = `.omp` agents `.md` 文件;sidecar 一次性迁移;增删改经 dev 栈真实走通
16. **persona 增删改** —— CRUD 往返实测;**选择器→会话生效链 08-22 修复**(materialize 此前查死 sidecar,persona 从未生效,证据 99 §6.17)
17. **会话树谱系** —— 真实分支父子关系拉取验证
18. **真实模型回合端到端** —— UI 发 "reply with exactly: ok" → GLM-5.3 回 "ok",transcript 双侧渲染(08-22 真机)
19. **`/undo` 预填** —— ✅ 08-22 闭环(额度恢复后):新会话两真实回合 → `/undo` → composer 预填被撤回文本 + transcript 叶回退 + "Reverted messages 1"(§6.12 服务端修复 UI 链路全通;证据 99 §6.13)
20. **压缩 loader** —— ✅ 08-22 闭环:`Compacting context…` 状态行真实渲染(观察员 4 捕获 + 窗口中点截图);此前"未捕获"根因 = 引擎默认 snapcompact 策略压缩仅 2-11ms 亚帧闪现,非 UI 缺陷;loader 为 auto-compaction 专用品(手动 /compact 不发 lifecycle 事件对,TUI 同语义)。证据 99 §6.13

| plan 评审弹层(四选一) | 需要真实 plan 运行到评审点 |
| goal 指示器 | 需要真实 goal 设置 |
| 模型降级徽标 | 没遇到过真实降级 |
| retry 提示 | 没遇到过真实失败 |
| local:// 链接点开查看器 | 点击级没驱动(需 transcript 含 local:// 链接) |
| subagent 运行行 | 没跑过真实子代理 |
| 托盘 / 系统通知裁决 | 需要 Electron 真机 |
| ask 答案卡(transcript 里) | 组件级测试过,没等来真实 ask 落卡 |
| WorkStatus「等待审批」阻塞态 | 单测过 |
| **Electron / VS Code / Mobile 三端** | 代码共享全在,**一次都没实跑** |

**缺陷(08-22 登记 → 08-22 晚修复)**:坏会话 transcript 崩溃/骨架——根因是 omp store 双通道写入竞态产生的"残缺 slice"(`seedSessionModel` 对无 slice 目录写出只有 sessionModel 的形状;retry 选择器按消息 id 索引 undefined map 即崩,且旧 `applyEvent` 无法自愈)。已修复(`3291d04`:空形状打底 + 选择器 `?.` 防御),单测 + 真实浏览器 CDP 复验零错误,证据 §6.13。骨架的加载恢复面同源解除,坏会话完整渲染留日常观察。

**新登记缺陷(08-22 午间,/compact 研究)**:① 手动 `/compact` 成功后 UI 不刷新 transcript —— **✅ 08-22 已修**:拦截器 resolve 后主动 `refetchSessionMessages`(刷新失败静默降级,不误报压缩失败);错误 toast 同时改为提取引擎嵌套 message(client.ts formatSdkError),真机双路径验证(200→divider 免刷新出现;500→toast 显示 "Nothing to compact (session too small)" 无 JSON 原文;client.test +1 契约测)。② 拒绝路径错误文案误导:prepareCompaction 对"上次压缩后无消息"与"历史不足 keepRecentTokens(默认 20K)"都报 "Nothing to compact (session too small)"(引擎侧文案,SDK 仓库,非本仓可改,留档)。③ sync 未连接时提交挂起:waitForConnectionOrThrow 无超时无提示(未修,极端态,独立待办)。

**验证方式**:日常使用遇到就点一下;不对即报。

## 第三档:一半 —— UI 侧已到极限,卡在引擎/服务端缺端点 🟡

> 「服务端好了、UI 没做」的半成品在批次 1–5 已清零(原 33 项全是该形态)。
> 现在剩的一半是反向:UI 到位、引擎缺端点。

| 功能 | 缺什么 |
|---|---|
| 定时任务 persona 字段 | UI 渲染了,引擎 prompt 不认 persona(发送无意义,故不发送) |
| goal 写操作(set/pause) | 引擎没有 goal 写端点,指示器只能只读 |
| 旧 agent 配置导入 | 引擎没暴露导入端点,UI 按契约不显示这块 |
| jobs 任务面 | 引擎未注入 ownerSessionID,capability 关着,UI 不亮 |
| drafts | 服务端端点未建 |
| `/redo` omp 化 | 只做了 /undo,redo 还是旧语义 |
| 跨目录设置显示 | 写入成功,另一目录实例要刷新才见(显示滞后,功能不坏) |
| TUI 组件类扩展面(footer/header/custom/Ink 工厂) | 09 章 R-E3 三级机制:dropped 可观测已上线;声明式渲染等上游序列化契约(开放问题 1) |

这 8 项的钥匙不在本仓库 —— 等 omp 引擎(`@oh-my-pi/pi-coding-agent`)补端点/契约。



## 第四档:根本没做 ❌

**旧面清理(10 项)—— 一刀未动,等观察门(≥09-03)后批次 6:**

权限盾牌按钮、旧权限卡片链、question 卡片链、AgentPermissionsEditor、shell `!` 通道、wire 死事件消费链、`/command` 空端点、plan 实验残迹文件、BehaviorPage 旧路径、share 复核

**P2 新功能(18 项)—— 下一阶段规划,未排期:**

Agent Hub、parked 会话复活、尾读、subagent HUD、artifacts 浏览、ssh/vault/security(前置=威胁评审)、vibe / loop / prewalk 面板 / btw / tan / goal 续跑共 6 项、消息撤回回收(message.part.removed)、逐轮完整渲染(ttsr/thinking)、usage / context 对齐、多客户端队列一致(等上游 SDK 扩展)

**批次 7 尾款(3 项)—— 可随时做,未排期:**

E02 StatusRow 扩展段消费面(host 侧已就绪)、E03 composer 预填(setEditorText)、E04 title / open_url

**杂项(3 项)—— ✅ 2026-08-21 已全部清零:**

readFileSync 脆测试重写(✅ ompComposerSurfaces.test.tsx 行为测)、master 文档状态行改口(✅)、`.jspace/` 进 gitignore(✅)

---

## 一屏总结
```
任务池 70 项 = 完成·已验 20 + 做完待验 10 + 一半卡引擎 8 + 没做 31 + 门 1
未完成 50 = 待验 10(等真实状态/三端)+ 卡引擎 8(等上游端点)
             + 没做 31(10 删等门 + 18 P2 未排期 + 3 尾款可随时做)+ 门 1(计时中)
```

**下一步**:① 剩余待真机项按日常使用顺手清(plan 评审弹层可用真实会话触发;goal 指示器需真实 goal 设置,而写端点在第三档卡引擎清单);② 观察门 09-03 起验(100 次弹窗结算,legacy 命中=0 / orphan<1% / 超时<1%);③ 过门后批次 6 删除列车;④ 批次 7 尾款 3 件与 P2 轮次按优先级开;⑤ 桌面端 capabilities 500(桌面 runtime 下 `/api/omp/capabilities`)择机排查。
