# omp-parity 任务进度总结(用户口径)

- **日期**:2026-08-21 初版 / **2026-08-22 更新**(批次 7 扩展宿主面 + 真机 UI 扫描 + 三缺陷修复)
- **基准**:批次 1–5 验收(08-20);批次 7 E01/E02/E08 落地并 live 验收(08-21 深夜);真机 CDP 扫描(08-22 凌晨);观察门计时中(起点 2026-08-20,最早 2026-09-03 验)
- **配套**:`00-MASTER.md` §8 批次计划(含批次 7 行、09 章);`99-ACCEPTANCE-EVIDENCE.md` §5–6 逐项总账与验收证据(最新 §6.12)
- **读法**:按「能不能用 + 测没测」分四档,不是按 spec 编号

---

## 第一档:界面上能完整体验,已亲自测试 ✅ tested

**聊天输入框 / 会话里:**

1. **审批弹窗** —— 非 yolo 会话跑工具会弹窗,批准 / 拒绝 / 「始终允许」(先写设置成功才批准)全部真实走通(浏览器驱动 + 修复 8 个缺陷后复验)
2. **ask 提问弹窗** —— 同上,真实 SDK 弹窗驱动过
3. **思考等级菜单** —— 选 Max → 落 transcript → 重载后输入框仍显示 "Max"(完整链路验证)
4. **模型选择器** —— 角色轨 + 全模型列表双面;enabledModels 白名单过滤 + 乱配警示行
5. **「设为角色」** —— UI 里把 tiny 角色指到某模型,`config.yml` 真实变更
6. **发消息不带模型污染** —— 角色跟随,抓包验证发送体无 model
7. **扩展 widget 投影**(批次 7)—— ✅ 08-22 真机视觉确认:输入框上方五行 zhipu 配额条逐字可见、实时自刷新;`/zhipu-usage off` 清除链路服务端验证
8. **`/zhipu-usage` 扩展命令识别**(08-22 修复)—— 用户扩展的 `pi.registerCommand` 命令进注册表(`source:"extension"`)+ 自动补全实测出现(此前 headless 发现机制天然不含扩展命令)
9. **persona 选择器菜单** —— ✅ 08-22 真机过:modes.v1 下 persona 区在模式菜单内,开合正常
10. **斜杠自动补全合并** —— ✅ 08-22 真机过:`/` 菜单含 `/tree`、`/troubleshoot`(引擎层)与 `/zhipu-usage`(扩展层)
11. **`/tree` 会话树对话框** —— ✅ 08-22 复核闭环:有回合会话 500ms 打开、内容稳定(此前"卡 Loading"为 Enter×菜单交互竞态误报)
12. **notice toast** —— ✅ 08-22 真机偶得:MCP 挂载通知以 toast 呈现(omp.notice.raised → toast 链路实跑)

**设置页:**

13. **引擎设置页** —— 各项修改真实写入 `config.yml` 落盘验证
14. **Defaults 角色编辑器** —— 10 个角色赋值/清除,服务端真值变化验证(含修复 Clear 按钮未绑 onClick 的缺陷)

**命令 / 数据面(curl 实测,界面消费):**

15. **自定义 agent 增删改** —— POST 后列表真值出现,删除干净
16. **persona 增删改** —— CRUD 往返实测
17. **会话树谱系** —— 真实分支父子关系拉取验证
18. **真实模型回合端到端** —— UI 发 "reply with exactly: ok" → GLM-5.3 回 "ok",transcript 双侧渲染(08-22 真机)

## 第二档:UI 全部接通,尚未测试(单测背书,等真机/等真实状态)⏸

| 功能 | 卡在哪 |
|---|---|
| `/undo` 预填视觉确认 | **服务端已修**(wire id→entry id 反解 + 加载竞态有界等待,单测+HTTP 层证实);浏览器复验受阻——验证会话被反复 revert 搞坏 + zhipu 配额打满。额度恢复后 2 分钟可验(新会话两回合 → /undo → composer 预填) |
| plan 评审弹层(四选一) | 需要真实 plan 运行到评审点 |
| goal 指示器 | 需要真实 goal 设置 |
| 模型降级徽标 | 没遇到过真实降级 |
| 压缩 loader | `/compact` 已提交但 loader 未捕获(采样窗口错过)——待复验 |
| retry 提示 | 没遇到过真实失败 |
| local:// 链接点开查看器 | 点击级没驱动(需 transcript 含 local:// 链接) |
| subagent 运行行 | 没跑过真实子代理 |
| 托盘 / 系统通知裁决 | 需要 Electron 真机 |
| ask 答案卡(transcript 里) | 组件级测试过,没等来真实 ask 落卡 |
| WorkStatus「等待审批」阻塞态 | 单测过 |
| **Electron / VS Code / Mobile 三端** | 代码共享全在,**一次都没实跑** |

**新登记缺陷(08-22)**:被反复 revert/重启的会话 transcript 永久骨架(UI 端消息加载不恢复;服务端 GET /message 200 正常)——独立待查,与 /undo 无关。

**验证方式**:日常使用遇到就点一下;不对即报。

## 第三档:一半 —— UI 侧已到极限,卡在引擎/服务端缺端点 🟡

> 「服务端好了、UI 没做」的半成品在批次 1–5 已清零(原 33 项全是该形态)。
> 现在剩的一半是反向:UI 到位、引擎缺端点。

| 功能 | 缺什么 |
|---|---|
| 定时任务 persona 字段 | UI 渲染了,引擎 prompt 不认 persona(发送无意义,故不发送) |
| goal 写操作(set/pause) | 引擎没有 goal 写端点,指示器只能只读 |
| 旧 agent 配置导入 | 引擎没暴露导入端点,UI 按契约不显示这块 |
| jobs 任务面 | 引擎未注入 ownerSessionID,capability 关着,UI 不亮 |
| drafts | 服务端端点未建 |
| `/redo` omp 化 | 只做了 /undo,redo 还是旧语义 |
| 跨目录设置显示 | 写入成功,另一目录实例要刷新才见(显示滞后,功能不坏) |
| TUI 组件类扩展面(footer/header/custom/Ink 工厂) | 09 章 R-E3 三级机制:dropped 可观测已上线;声明式渲染等上游序列化契约(开放问题 1) |

这 8 项的钥匙不在本仓库 —— 等 omp 引擎(`@oh-my-pi/pi-coding-agent`)补端点/契约。

**批次 7 尾款**(随车项,未到期):E02 StatusRow 消费面(host 就绪)、E03/E04 预填/title/open_url。

## 第四档:根本没做 ❌

**旧面清理(10 项)—— 一刀未动,等观察门(≥09-03)后批次 6:**

权限盾牌按钮、旧权限卡片链、question 卡片链、AgentPermissionsEditor、shell `!` 通道、wire 死事件消费链、`/command` 空端点、plan 实验残迹文件、BehaviorPage 旧路径、share 复核

**P2 新功能(18 项)—— 下一阶段规划,未排期:**

Agent Hub、parked 会话复活、尾读、subagent HUD、artifacts 浏览、ssh/vault/security(前置=威胁评审)、vibe / loop / prewalk 面板 / btw / tan / goal 续跑共 6 项、消息撤回回收(message.part.removed)、逐轮完整渲染(ttsr/thinking)、usage / context 对齐、多客户端队列一致(等上游 SDK 扩展)

**杂项(3 项)—— ✅ 2026-08-21 已全部清零:**

readFileSync 脆测试重写(✅ ompComposerSurfaces.test.tsx 行为测)、master 文档状态行改口(✅)、`.jspace/` 进 gitignore(✅)

---

## 一屏总结

```
能用且测过         18 个功能点   ← 打开界面就是好的(08-22 新增:widget 视觉、/zhipu-usage、
                                   /tree 对话框、persona 菜单、自动补全、notice、真实回合)
接通了没测         12 个功能点   ← /undo 待额度恢复复验(服务端已修);其余等真实状态/三端
一半(卡引擎)      8 项         ← +TUI 组件扩展面(等上游契约);批次 7 尾款随车
没做              28 项        ← 10 删除(等观察门)+ 18 新功能(P2);杂项已清零
```

**下一步**:① zhipu 额度恢复后复验 `/undo` 预填 + 顺手清第二批待真机项(plan/goal/compaction 都可用真实会话触发);② 查"坏会话 transcript 骨架"缺陷;③ 观察门 09-03 起验(100 次弹窗结算,legacy 命中=0 / orphan<1% / 超时<1%);④ 过门后批次 6 删除列车;⑤ P2 轮次按优先级开。
